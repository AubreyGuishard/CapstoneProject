import React from 'react';
const InputRegisterPage = ({value, onChange, label, type = 'text', username, firstname, lastname, email, password}) => {
    return ( 
        <div>
            <label></label>
        </div>
     );
}
 
export default InputRegisterPage;